import { FormBuilder } from "@/components/form-builder"

export default function BuilderPage() {

  return (
    <main className="min-h-screen">
      <FormBuilder initialFormData={undefined} />
    </main>
  )
}
